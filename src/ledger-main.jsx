import React from "react";
import ReactDOM from "react-dom/client";
import LedgerDashboard from "./LedgerDashboard.jsx";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <LedgerDashboard />
  </React.StrictMode>
);
